
def get(kleur, img):
    # gooi mask (afhankelijk van kleur) over img
    # if kleur == 1:
    #     img_masked = mask geel
    # if kleur == 2:
    #     img_masked = mask rood
    # etc
    img_masked = img
    return img_masked

